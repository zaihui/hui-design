export type ToastTypeProp = 'success' | 'fail' | 'warning' | 'text' | 'custom'

export enum ToastTypeEnum {
  SUCCESS = 'success',
  FAIL = 'fail',
  WARNING = 'warning',
  TEXT = 'text',
  CUSTOM = 'custom'
}
